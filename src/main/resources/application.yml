spring:
  application:
    name: noveo-test-assignment

  datasource:
    url: jdbc:postgresql://localhost:5432/noveo
    driver-class-name: org.postgresql.Driver
    username: username
    password: password

  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
